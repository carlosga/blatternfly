@namespace Blatternfly.Components
@inherits BaseComponent

<div
  @attributes="AdditionalAttributes"
  class="@CssClass"
  role="dialog"
  aria-modal="true"
>
  @ChildContent
</div>

@code {
  /// PopoverDialog position.
  [Parameter] public PopoverPosition? Position { get; set; } = PopoverPosition.Top;

  private string CssClass => new CssBuilder("pf-c-popover")
    .AddClass("pf-m-top"   , Position is null || Position == PopoverPosition.Top)
    .AddClass("pf-m-bottom", Position is not null && Position == PopoverPosition.Bottom)
    .AddClass("pf-m-left"  , Position is not null && Position == PopoverPosition.Left)
    .AddClass("pf-m-right" , Position is not null && Position == PopoverPosition.Right)
    .AddClassFromAttributes(AdditionalAttributes)
    .Build();
}