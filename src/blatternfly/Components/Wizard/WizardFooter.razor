@namespace Blatternfly.Components

<footer class="pf-c-wizard__footer">@ChildContent</footer>

@code {
  /// Content rendered inside the component.
  [Parameter] public RenderFragment ChildContent { get; set; }
}