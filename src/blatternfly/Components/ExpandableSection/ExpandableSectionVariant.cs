namespace Blatternfly.Components;

/// <summary>Expandable section variants.</summary>
public enum ExpandableSectionVariant
{
    /// <summary>Default.</summary>
    Default,

    /// <summary>Truncates the expandable content to the especified <see cref="ExpandableSection.TruncateMaxLines" /> number of lines.</summary>
    Truncate
}