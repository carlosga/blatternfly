@namespace Blatternfly.Components
@inherits BaseComponent

@if (IsHorizontal)
{
<button class="pf-c-nav__scroll-button" aria-label="@AriaLeftScroll" @onclick="ScrollLeft" disabled="@_scrollViewAtStart">
  <AngleLeftIcon />
</button>
}
<ul class="pf-c-nav__list" @attributes="AdditionalAttributes" @onscroll="HandleScrollButtons">
  @ChildContent
</ul>
@if (IsHorizontal)
{
<button class="pf-c-nav__scroll-button" aria-label="@AriaRightScroll" @onclick="ScrollRight" disabled="@_scrollViewAtEnd">
  <AngleRightIcon />
</button>
}

@code {
  /// Parent Nav control
  [CascadingParameter] public Nav Parent { get; set; }

  /// aria-label for the left scroll button.
  [Parameter] public string AriaLeftScroll { get; set; } = "Scroll left";

  /// aria-label for the right scroll button.
  [Parameter] public string AriaRightScroll { get; set; } = "Scroll right";

  private bool IsHorizontal         { get => HasHorizontalVariant || HasTertiaryVariant; }
  private bool HasHorizontalVariant { get => Parent.Variant == NavVariant.Horizontal; }
  private bool HasTertiaryVariant   { get => Parent.Variant == NavVariant.Tertiary; }

  private bool _scrollViewAtStart = false;
  private bool _scrollViewAtEnd = false;

  private void HandleScrollButtons()
  {
  }

  private void ScrollLeft()
  {
  }

  private void ScrollRight()
  {
  }
}