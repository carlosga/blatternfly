<Page>
  <Header>
    <Masthead>
      <MastheadToggle>
        <PageToggleButton
          AriaLabel="Global navigation"
          IsNavOpen="@IsNavOpen"
          OnNavToggle="@OnNavToggle"
        >
          <BarsIcon />
        </PageToggleButton>
      </MastheadToggle>
      <MastheadMain>
        <MastheadBrand href="https://patternfly.org" target="_blank">
          Logo
        </MastheadBrand>
      </MastheadMain>
      <MastheadContent>
        <Toolbar id="with-or-witout-fill-toolbar">
          <ToolbarContent>
            <ToolbarItem>header-tools</ToolbarItem>
          </ToolbarContent>
        </Toolbar>
      </MastheadContent>
    </Masthead>
  </Header>
  <Sidebar>
    <PageSidebar nav="Navigation" IsNavOpen="@IsNavOpen" />
  </Sidebar>
  <Content>
    <PageSection>Section with default padding</PageSection>
    <PageSection Variant="PageSectionVariants.Light" Padding="@Section1Padding">
      Section with no padding
    </PageSection>
    <PageSection Padding="@Section2Padding">
      Section with padding only on medium/large
    </PageSection>
    <PageSection Variant="PageSectionVariants.Light" Padding="@Section3Padding">
      Section with no padding on medium
    </PageSection>
  </Content>
</Page>

@code {
  public PaddingModifiers Section1Padding => new() { Default = Padding.NoPadding };
  public PaddingModifiers Section2Padding => new() { Default = Padding.NoPadding, Medium = Padding.Padding, Large = Padding.Padding };
  public PaddingModifiers Section3Padding => new() { Medium  = Padding.NoPadding };

  public bool IsNavOpen { get; set; } = true;

  public void OnNavToggle(bool value)
  {
    IsNavOpen = value;
  }
}