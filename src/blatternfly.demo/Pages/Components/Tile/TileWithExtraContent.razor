<div role="listbox" aria-label="Tiles with extra content">
  <Flex>
    <Flex FlexShorthand="@(new () { Default = FlexState.Flex1 })">
      <Tile Title="Default" IsStacked IsSelected="false">
        <Icon>
          <BellIcon />
        </Icon>
        <ChildContent>
          This is really really long subtext that goes on for so long that it has to wrap to the next line. This is really
          really long subtext that goes on for so long that it has to wrap to the next line.
        </ChildContent>
      </Tile>
    </Flex>
    <Flex FlexShorthand="@(new () { Default = FlexState.Flex1 })">
      <Tile Title="Selected" IsStacked IsSelected>
        <Icon>
          <BellIcon />
        </Icon>
        <ChildContent>
          This is really really long subtext that goes on for so long that it has to wrap to the next line.
        </ChildContent>
      </Tile>
    </Flex>
    <Flex FlexShorthand="@(new () { Default = FlexState.Flex1 })">
      <Tile Title="Disabled" IsStacked IsDisabled IsSelected="false">
        <Icon>
          <BellIcon />
        </Icon>
        <ChildContent>
          Subtext goes here
        </ChildContent>
      </Tile>
    </Flex>
  </Flex>
</div>