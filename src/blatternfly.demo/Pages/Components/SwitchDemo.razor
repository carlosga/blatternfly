@page "/components/switch"

<TextContent>
  <Title class="pf-u-mb-md">Switch</Title>
  <Text class="pf-u-mb-md">
    A <strong>switch</strong> toggles the state of a setting (between on and off).
  </Text>
  <Text class="pf-u-mb-md">
    See the <a target="_blank" href="https://www.patternfly.org/v4/components/switch/design-guidelines">PatternFly design guidelines</a> for more details.
  </Text>
</TextContent>

<Flex>
  <FlexItem Component="nav" class="ws-toc">
    <Title Size="TitleSizes.Large">Table of contents</Title>
    <JumpLinks IsVertical ScrollableSelector=".pf-c-page__main" OffsetSelector=".pf-c-page__header">
      <JumpLinksItem Href="#examples">
        <ChildContent>Examples</ChildContent>
        <SubLists>
          <JumpLinksList>
            <JumpLinksItem Href="#basic">Basic</JumpLinksItem>
            <JumpLinksItem Href="#reversed-layout">Reversed Layout</JumpLinksItem>
            <JumpLinksItem Href="#without-label">Without label</JumpLinksItem>
            <JumpLinksItem Href="#disabled">Disabled</JumpLinksItem>
          </JumpLinksList>
        </SubLists>
      </JumpLinksItem>
    </JumpLinks>
  </FlexItem>
  <FlexItem Grow="@(new FlexGrow { Default = true })">
    <Title id="examples" Size="TitleSizes.Large">Examples</Title>

    <section id="basic" class="example-section">
      <Title Size="TitleSizes.Large">Basic</Title>
      <Switch
        id="simple-switch"
        @bind-Value="_switch1"
      >
        <Label>Message when on</Label>
        <LabelOff>Message when off</LabelOff>
      </Switch>
    </section>

    <section id="reversed-layout" class="example-section">
      <Title Size="TitleSizes.Large">Reversed Layout</Title>
      <Switch
        id="reversed-switch"
        @bind-Value="_switch2"
        IsReversed
      >
        <Label>Message when on</Label>
        <LabelOff>Message when off</LabelOff>
      </Switch>
    </section>

    <section id="without-label" class="example-section">
      <Title Size="TitleSizes.Large">Without label</Title>
      <Switch id="no-label-switch-on" AriaLabel="Message when on" @bind-Value="_switch3" />
    </section>

    <section class="example-section">
      <Title Size="TitleSizes.Large">Disabled</Title>
      <Switch id="disabled-switch-on" AriaLabel="Message when on" Value="true" IsDisabled>
        <Label>Message when on</Label>
        <LabelOff>Message when off</LabelOff>
      </Switch>
      <br />
      <Switch
        id="disabled-switch-off"
        AriaLabel="Message when on"
        Value="false"
        IsDisabled
      >
        <Label>Message when on</Label>
        <LabelOff>Message when off</LabelOff>
      </Switch>
      <br />
      <Switch id="disabled-no-label-switch-on" AriaLabel="Message when on" Value="true" IsDisabled />
      <br />
      <Switch id="disabled-no-label-switch-off" AriaLabel="Message when on" Value="false" IsDisabled />
    </section>
  </FlexItem>
</Flex>

@code {
    private bool _switch1 = true;
    private bool _switch2 = true;
    private bool _switch3 = true;
}