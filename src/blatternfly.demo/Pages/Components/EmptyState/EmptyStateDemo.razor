@page "/components/empty-state"

<TextContent>
  <Title class="pf-u-mb-md">Empty state</Title>
  <Text class="pf-u-mb-md">
    An <strong>empty state</strong> is a screen that is not yet populated with data or information.
  </Text>
  <Text class="pf-u-mb-md">
    See the <a target="_blank" href="https://www.patternfly.org/v4/components/empty-state/design-guidelines">PatternFly design guidelines</a> for more details.
  </Text>
</TextContent>

<Flex>
  <FlexItem Component="nav" class="ws-toc">
    <Title Size="TitleSizes.Large">Table of contents</Title>
    <JumpLinks IsVertical ScrollableSelector=".pf-c-page__main" OffsetSelector=".pf-c-page__header">
      <JumpLinksItem Href="#examples">
        <ChildContent>Examples</ChildContent>
        <SubLists>
          <JumpLinksList>
            <JumpLinksItem Href="#basic">Basic</JumpLinksItem>
            <JumpLinksItem Href="#extra-small">Extra Small</JumpLinksItem>
            <JumpLinksItem Href="#small">Small</JumpLinksItem>
            <JumpLinksItem Href="#large">Large</JumpLinksItem>
            <JumpLinksItem Href="#extra-large">Extra large</JumpLinksItem>
            <JumpLinksItem Href="#spinner">Spinner</JumpLinksItem>
            <JumpLinksItem Href="#no-match-found">No match found</JumpLinksItem>
          </JumpLinksList>
        </SubLists>
      </JumpLinksItem>
    </JumpLinks>
  </FlexItem>
  <FlexItem Grow="@(new FlexGrow { Default = true })">
    <Title id="examples" Size="TitleSizes.Large">Examples</Title>

    <section id="basic" class="example-section">
      <Title Size="TitleSizes.Large">Basic</Title>
      <EmptyState>
        <EmptyStateIcon>
          <CubesIcon aria-hidden="true" />
        </EmptyStateIcon>
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Large">Empty state</Title>
        <EmptyStateBody>
          This represents an the empty state pattern in Patternfly 4. Hopefully it's simple enough to use but flexible
          enough to meet a variety of needs.
        </EmptyStateBody>
        <Button Variant="ButtonVariant.Primary">Primary action</Button>
        <EmptyStateSecondaryActions>
          <Button Variant="ButtonVariant.Link">Multiple</Button>
          <Button Variant="ButtonVariant.Link">Action Buttons</Button>
          <Button Variant="ButtonVariant.Link">Can</Button>
          <Button Variant="ButtonVariant.Link">Go here</Button>
          <Button Variant="ButtonVariant.Link">In the secondary</Button>
          <Button Variant="ButtonVariant.Link">Action area</Button>
        </EmptyStateSecondaryActions>
      </EmptyState>
    </section>

    <section id="extra-small" class="example-section">
      <Title Size="TitleSizes.Large">Extra Small</Title>
      <EmptyState Variant="EmptyStateVariant.ExtraSmall">
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Medium">Empty state</Title>
        <EmptyStateBody>
          This represents an the empty state pattern in Patternfly 4. Hopefully it's simple enough to use but flexible
          enough to meet a variety of needs.
        </EmptyStateBody>
        <EmptyStateSecondaryActions>
          <Button Variant="ButtonVariant.Link">Multiple</Button>
          <Button Variant="ButtonVariant.Link">Action Buttons</Button>
          <Button Variant="ButtonVariant.Link">Can</Button>
          <Button Variant="ButtonVariant.Link">Go here</Button>
          <Button Variant="ButtonVariant.Link">In the secondary</Button>
          <Button Variant="ButtonVariant.Link">Action area</Button>
        </EmptyStateSecondaryActions>
      </EmptyState>
    </section>

    <section id="small" class="example-section">
      <Title Size="TitleSizes.Large">Small</Title>
      <EmptyState Variant="EmptyStateVariant.Small">
        <EmptyStateIcon>
          <CubesIcon aria-hidden="true" />
        </EmptyStateIcon>
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Large">Empty state</Title>
        <EmptyStateBody>
          This represents an the empty state pattern in Patternfly 4. Hopefully it's simple enough to use but flexible
          enough to meet a variety of needs.
        </EmptyStateBody>
        <Button Variant="ButtonVariant.Primary">Primary action</Button>
        <EmptyStateSecondaryActions>
          <Button Variant="ButtonVariant.Link">Multiple</Button>
          <Button Variant="ButtonVariant.Link">Action Buttons</Button>
          <Button Variant="ButtonVariant.Link">Can</Button>
          <Button Variant="ButtonVariant.Link">Go here</Button>
          <Button Variant="ButtonVariant.Link">In the secondary</Button>
          <Button Variant="ButtonVariant.Link">Action area</Button>
        </EmptyStateSecondaryActions>
      </EmptyState>
    </section>

    <section id="large" class="example-section">
      <Title Size="TitleSizes.Large">Large</Title>
      <EmptyState Variant="EmptyStateVariant.Large">
        <EmptyStateIcon>
          <CubesIcon aria-hidden="true" />
        </EmptyStateIcon>
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Large">Empty state</Title>
        <EmptyStateBody>
          This represents an the empty state pattern in Patternfly 4. Hopefully it's simple enough to use but flexible
          enough to meet a variety of needs.
        </EmptyStateBody>
        <Button Variant="ButtonVariant.Primary">Primary action</Button>
        <EmptyStateSecondaryActions>
          <Button Variant="ButtonVariant.Link">Multiple</Button>
          <Button Variant="ButtonVariant.Link">Action Buttons</Button>
          <Button Variant="ButtonVariant.Link">Can</Button>
          <Button Variant="ButtonVariant.Link">Go here</Button>
          <Button Variant="ButtonVariant.Link">In the secondary</Button>
          <Button Variant="ButtonVariant.Link">Action area</Button>
        </EmptyStateSecondaryActions>
      </EmptyState>
    </section>

    <section id="extra-large" class="example-section">
      <Title Size="TitleSizes.Large">Extra large</Title>
      <EmptyState Variant="EmptyStateVariant.ExtraLarge">
        <EmptyStateIcon>
          <CubesIcon aria-hidden="true" />
        </EmptyStateIcon>
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Large">Empty state</Title>
        <EmptyStateBody>
          This represents an the empty state pattern in Patternfly 4. Hopefully it's simple enough to use but flexible
          enough to meet a variety of needs.
        </EmptyStateBody>
        <Button Variant="ButtonVariant.Primary">Primary action</Button>
        <EmptyStateSecondaryActions>
          <Button Variant="ButtonVariant.Link">Multiple</Button>
          <Button Variant="ButtonVariant.Link">Action Buttons</Button>
          <Button Variant="ButtonVariant.Link">Can</Button>
          <Button Variant="ButtonVariant.Link">Go here</Button>
          <Button Variant="ButtonVariant.Link">In the secondary</Button>
          <Button Variant="ButtonVariant.Link">Action area</Button>
        </EmptyStateSecondaryActions>
      </EmptyState>
    </section>

    <section id="spinner" class="example-section">
      <Title Size="TitleSizes.Large">Spinner</Title>
      <EmptyState>
        <EmptyStateIcon>
          <span class="pf-c-spinner" role="progressbar" aria-valuetext="Loading...">
            <span class="pf-c-spinner__clipper" />
            <span class="pf-c-spinner__lead-ball" />
            <span class="pf-c-spinner__tail-ball" />
          </span>
        </EmptyStateIcon>
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Large">Loading</Title>
      </EmptyState>
    </section>

    <section id="no-match-found" class="example-section">
      <Title Size="TitleSizes.Large">No match found</Title>
      <EmptyState>
        <EmptyStateIcon>
          <SearchIcon aria-hidden="true" />
        </EmptyStateIcon>
        <Title HeadingLevel="HeadingLevel.h4" Size="TitleSizes.Large">No results found</Title>
        <EmptyStateBody>
          No results match the filter criteria. Clear all filters and try again.
        </EmptyStateBody>
        <EmptyStatePrimary>
          <Button Variant="ButtonVariant.Link">Clear all filters</Button>
        </EmptyStatePrimary>
      </EmptyState>
    </section>
  </FlexItem>
</Flex>